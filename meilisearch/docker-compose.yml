services:
  meilisearch:
    image: getmeili/meilisearch
    restart: unless-stopped
    volumes:
      - /opt/meilisearch:/meili_data
    environment:
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY}
      MEILI_NO_ANALYTICS: true