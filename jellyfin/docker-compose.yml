services:
  jellyfin:
    image: jellyfin/jellyfin
    container_name: jellyfin
    network_mode: container:gluetun
    volumes:
      - ./config:/config
      - ./cache:/cache
      - ${MEDIA}:/media
    restart: 'unless-stopped'