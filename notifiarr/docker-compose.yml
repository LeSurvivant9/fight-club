services:
  notifiarr:
    container_name: notifiarr
    image: golift/notifiarr:latest
    mem_limit: 512M
    network_mode: container:gluetun
    environment:
      - TZ=${TIMEZONE}
    volumes:
      - ./config:/config
      - /var/run/utmp:/var/run/utmp
      - /etc/machine-id:/etc/machine-id
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "cat < /dev/null > /dev/tcp/localhost/5454 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    labels:
      - autoheal=true
      - autoheal.stop.timeout=30