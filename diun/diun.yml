watch:
  workers: 20
  schedule: "0 */1 * * *"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    watchByDefault: true

notif:
  discord:
    webhookURL: ${DIUN_DISCORD_WEBHOOK}
    renderFields: true
    timeout: 10s
    templateBody: |
      🚀 Nouvelle image disponible : **{{ .Entry.Image }}**
      🔎 Source : {{ .Entry.Provider }}
      📦 Tag : {{ .Entry.ImageDigest }}
      📅 Détectée à {{ .Entry.Created }}
